# Otel Foto Sistemi - backend bagimliliklari
# Kurulum (backend/ klasorunden, venv aktifken):
#     pip install -r requirements.txt

# --- Web / API ---
fastapi==0.139.0
uvicorn==0.49.0
python-multipart==0.0.20        # foto yukleme (multipart/form-data) icin

# --- Veritabani ---
SQLAlchemy==2.0.51
alembic==1.18.5                 # veritabani migration araci
psycopg2-binary==2.9.12         # PostgreSQL surucusu
pydantic==2.13.4

# --- Yuz tanima / goruntu isleme ---
insightface==1.0.1
onnxruntime-gpu==1.23.2
opencv-python==5.0.0.93
numpy==2.2.6
